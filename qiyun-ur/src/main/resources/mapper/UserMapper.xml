<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.ur.mapper.UserMapper">

    <!-- 判断用户名是否存在 -->
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1)
        FROM ur_user
        WHERE account = #{account}
    </select>

    <!-- 新增用户 -->
    <insert id="insertUser" parameterType="com.ruoyi.ur.domain.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO ur_user (
            account, password, status, create_time
        ) VALUES (
                     #{account}, #{password}, #{status}, #{createTime}
                 )
    </insert>

</mapper>
